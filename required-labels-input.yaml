LABELS:
  - owner
  - application

LIST:
  environment:
    name: environment
    list: [dev, staging, prod]

REGEX:
  version:
    name: version
    regex: ^[0-9]\.[0-9]\.[0-9]$

RESOURCES:
  compute: [beta.compute.instances.insert]
  bigquery: [v2.bigquery.dataset.insert, v2.bigquery.dataset.delete]

to_address: vinsonr
topic: c7n-notifications
project_id: prod-sh-t1-app-300917
